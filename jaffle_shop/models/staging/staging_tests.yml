version: 2

models:
  # Tests for the stg_customers model
  - name: stg_customers
    description: One unique customer per row.
    columns:
      - name: customer_id
        description: Primary key.
        tests:
          - unique
          - not_null

  # Tests for the stg_orders model
  # Make sure this '-' is at the same indentation level as the one for stg_customers
  - name: stg_orders
    description: One unique order per row.
    columns:
      - name: order_id
        description: Primary key.
        tests:
          - unique
          - not_null