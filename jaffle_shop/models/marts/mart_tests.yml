version: 2

models:
  - name: dim_customers
    description: One unique customer per row containing some basic spend information like last purchase date, total order amount, etc.
    columns: 
      - name: customer_id
        description: Primary key.
        tests:
          - unique
          - not_null